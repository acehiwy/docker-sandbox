schemaVersion: "2"
kind: mixin
name: openrouter-template
    
permissions:
  network:
    allow:
      - openrouter.ai

credentials:
  - service: openrouter-template
    apiKey:
      inject:
        - domain: opencode.ai
          scheme: bearer