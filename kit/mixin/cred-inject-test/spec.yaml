schemaVersion: "1"
kind: mixin
name: cred-inject-test

network:
  allowedDomains:
    - "localhost:23000"
  serviceDomains:
    "localhost:23000": path1
  serviceAuth:
    path1:
      headerName: Path1
      valueFormat: "Path1 %s"
      